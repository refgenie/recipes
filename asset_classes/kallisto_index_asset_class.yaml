name: kallisto_index
version: 0.1.0
description: Genome index for kallisto, produced with kallisto index
seek_keys:
  kallisto_index:
    value: "."
    type: directory
    description: Directory containing the kallisto index

  version:
    type: string
    description: "Kallisto version used to build the index"
parents: []
