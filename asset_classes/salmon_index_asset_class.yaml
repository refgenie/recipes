name: salmon_index
version: 0.1.0
description: Transcriptome index for salmon, produced with salmon index
seek_keys:
  salmon_index:
    value: "."
    type: directory
    description: Directory containing the Salmon index
  version:
    type: string
    description: "Salmon version used to build the index"
parents: []
