name: suffixerator_index
version: 0.1.0
description:
  Enhanced suffix array index for genomes using gt (GenomeTools) suffixerator
  program
seek_keys:
  esa:
    value: "{genome}.sft"
    type: file
    description: Suffix array file
  version:
    type: string
    description: "Suffixerator version used to build the index"
parents: []
