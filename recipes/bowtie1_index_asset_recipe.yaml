name: bowtie1_index
version: 0.0.1
output_asset_class: bowtie1_index
description: Genome index for bowtie, produced with bowtie-build
input_files: null
input_params:
  threads:
    description: Number of threads to use
    default: 1
input_assets:
  fasta:
    asset_class: fasta
    description: fasta asset for genome
    default: fasta
docker_image: docker.io/databio/refgenie
command_templates:
  - bowtie-build --threads {{values.params["threads"]}} {{values.assets["fasta"].seek_keys_dict["fasta"]}} {{values.output_folder}}/{{values.genome_digest}}
custom_properties:
  version: bowtie-build --version | awk 'NR==1{print $3}'
default_asset: "{{values.custom_properties.version}"
